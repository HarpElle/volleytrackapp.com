# volleytrackapp.com — status log (newest first)

## 2026-09-04 — Claude Code (Fable 5.1 review)

- Read-only review of a fresh clone of `main` (`c25e410`): all 13 pages,
  `assets/site.js` and `site.css`, `tokens/`, sitemap and robots, GitHub
  Pages config, PRs, branches, Dependabot, and the live site. Findings in
  `status/REVIEW_2026-09-04.md`: 0 P0, 3 P1 (mailto-only forms that confirm
  success they cannot verify; Google Fonts as an undisclosed third-party
  call), 7 P2 (dead `tokens/`, about 9 MB of unreferenced images,
  em-dashes, "Soon" help cards, no og:images, inline styles, unverified
  dirty local file).
- Rewrote `PLAN.md` "Next" into nine sized steps with acceptance checks,
  added triggers to "Later", and clarified Intentions (no analytics or
  tracking). Health moved from `idle` to `attention` because the deletion
  form is the URL declared to Google Play. `CURRENT.md` and `status.json`
  updated to match, with four decisions for Jason.
- Checked the sibling app repo: VolleyTrack PR #51 merged 2026-09-04, so the
  store-submission date is the trigger for the launch flip (step 5).

## 2026-09-04 — Claude Code (Fable 5.1)

- Created the `status/` folder from the 2026-09-04 portfolio sweep
  (GitHub commits, PRs, check runs, and the Primary Mac checkout).
- Recorded stage `live`, health `idle`, 0 item(s) in flight,
  0 blocker(s), 0 decision(s) for Jason.
- Codex is asked to review this record against its own recent sessions and
  correct anything it knows better.
